import { Meta } from "@storybook/blocks"
import { Flex } from "../Flex"
import { Link } from "../Link"
import { Text } from "../Text"

<Meta title="Componentes/Flex/Guia de Uso" />

# 🧭  Guia de Uso

## 📘 Visão Geral

O componente `Flex` organiza elementos de forma flexível e responsiva, utilizando o modelo de *layout* Flexbox do CSS. Ele permite alinhar, distribuir e espaçar elementos de maneira eficiente, adaptando-se a diferentes tamanhos de tela e resoluções por meio de suas propriedades baseadas em classes do Tailwind CSS.

<Flex align="items-center" justify="justify-center" direction="flex-col">
  <Flex direction="flex-col" justify="justify-between" align="items-center" wrap="flex-nowrap" gap="gap-sm" width="w-full" className="max-w-164 md:flex-row">
    <div className="bg-primary p-lg rounded-lg w-full text-center">
      <Text className="!text-typography-inverted" weight="font-semibold">Item 1</Text>
    </div>
    <div className="bg-primary p-lg rounded-lg w-full text-center">
      <Text className="!text-typography-inverted" weight="font-semibold">Item 2</Text>
    </div>
    <div className="bg-primary p-lg rounded-lg w-full text-center">
      <Text className="!text-typography-inverted" weight="font-semibold">Item 3</Text>
    </div>
  </Flex>
  <Text className="!mb-0">Exemplo de aplicação de Flexbox com o componente `Flex`,
    onde os itens são distribuídos horizontalmente com espaçamento uniforme e alinhamento centralizado a partir das propriedades e classes disponibilizadas pelo componente.</Text>
</Flex>

## 💡 Recomendações de Uso

- Utilize o componente `Flex` para criar *layouts* responsivos e flexíveis, permitindo que os elementos se ajustem automaticamente ao tamanho da tela;
- Aproveite as propriedades de alinhamento (`align`), justificação (`justify`) e direção (`direction`) para controlar a disposição dos elementos dentro do contêiner flexível;
- Use a propriedade `wrap` para permitir que os elementos quebrem a linha e se movam para a próxima, quando não houver espaço suficiente, garantindo uma melhor organização visual;
- Utilize a propriedade `gap` para definir o espaçamento entre os elementos, melhorando a legibilidade e a estética do *layout*;
- Teste o *layout* em diferentes tamanhos de tela e resoluções para garantir que a disposição dos elementos seja adequada e responsiva em todas as situações. Considere também o uso de *breakpoints* para ajustar o *layout* em tamanhos específicos, garantindo uma experiência de usuário consistente e agradável.

## ♿ Acessibilidade

O componente `Flex` foi desenvolvido e testado para atender às diretrizes WCAG 2.1, oferecendo suporte a propriedades de acessibilidade e práticas recomendadas para um conteúdo acessível. Para garantir que o uso do componente `Flex` esteja em conformidade com as diretrizes de acessibilidade, é importante seguir algumas práticas recomendadas:

- Use o componente Flex para organizar elementos de forma que a ordem visual reflita a ordem lógica do conteúdo, garantindo que leitores de tela possam interpretar corretamente a estrutura da página. Isso está alinhado com a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/meaningful-sequence.html" target="_blank">WCAG 1.3.2 (Meaningful Sequence)</Link>;
- Evite o uso excessivo de elementos `div` ou `span` sem propósito semântico. Sempre que possível, utilize a propriedade `component` para alterar o elemento renderizado e incluir tags HTML semânticas apropriadas (como `header`, `nav`, `main`, `section`, `article`, etc.), melhorando a compreensão por tecnologias assistivas, conforme a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/info-and-relationships.html" target="_blank">WCAG 1.3.1 (Info and Relationships)</Link>;
- Utilize classes de acessibilidade do Tailwind CSS, como `sr-only`, para ocultar visualmente conteúdos que devem permanecer acessíveis a leitores de tela, garantindo a entrega de informações completas a todos os usuários, conforme a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/non-text-content.html" target="_blank">WCAG 1.1.1 (Non-text Content)</Link>.