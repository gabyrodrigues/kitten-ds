import { Meta } from "@storybook/blocks"
import { Link } from "../Link"

<Meta title="Componentes/Card/Guia de Uso" />

# 🧭  Guia de Uso

## 📘 Visão Geral

O `Card` fornece um elemento de superfície para agrupar informações relacionadas, como texto, imagens e ações, em um único bloco visual. Ele é ideal para exibir conteúdo de forma organizada e atraente, facilitando a compreensão e a interação do usuário com as informações apresentadas.

### ⚙️ Tipos de Card

O componente `Card` possui as seguintes variações:

- **Apenas leitura**: Versão padrão do `Card`, que apenas agrupa as informações contidas. Ele pode possui elementos interativos dentro dele, como um link ou botão, mas não há nenhum tipo de interação no `Card` propriamente dito;
- **Interativo**: Versão do `Card` que o torna interativo, podendo assumir as características de:
  - **Link**: Redirecionando o usuário para uma nova página ou seção ao clicar;
  - **Selecionável**: O `Card` pode ser selecionado, permitindo que o usuário escolha uma ou mais opções de um conjunto de `Cards`. A versão selecionável também pode ser desabilitada, evitando que o usuário interaja com ela caso esteja indisponível no momento.

O componente também pode possuir as seguintes variações estilo:

- **Com borda**: O `Card` pode exibir uma borda ao seu redor, a partir da propriedade `hasBorder`, destacando-o do fundo e criando uma separação visual com outros elementos. Isso é útil para enfatizar o conteúdo do `Card` e torná-lo mais distinto;
- **Com sobreamento**: Por padrão, o `Card` exibe uma sombra ao seu redor, a partir da propriedade `hasShadow`, criando um efeito de elevação e destacando-o do fundo;
- **Estado de carregamento**: O `Card` pode ser exibido em um estado de carregamento, através da propriedade `isLoading`, que exibe uma animação de pulsação. Isso é útil para indicar que o conteúdo está sendo carregado ou processado, funcionando como um elemento de esqueleto, para que o usuário entenda que a informação ainda não está disponível.

### 🧱 Estrutura

A estrutura básica de um `Card` é composta pelos seguintes elementos:

- **Cabeçalho**: Área destinada ao título e outras informações adicionais de destaque;
- **Conteúdo**: Área onde o conteúdo principal é exibido, podendo incluir texto, imagens e outros elementos;
- **Rodapé**: Área opcional para informações ou ações adicionais.

## 💡 Recomendações de Uso

- Utilize o `Card` para agrupar informações relacionadas de forma clara e organizada;
- Ao utilizar cards em conjunto, mantenha as larguras padronizadas de um card para o outro;
- Prefira a versão interativa do `Card` quando for necessário permitir a interação do usuário, como seleção ou navegação;
- Aproveite as variações de estilo para destacar informações importantes ou criar hierarquia visual;
- Organize-os de forma simples, pois a apresentação dos cards tem impacto no modo em como são compreendidos;
- Considere o uso do estado de carregamento para melhorar a experiência do usuário durante a espera por conteúdo.
- Evite inserir elementos interativos ao utilizar o `Card` em sua versão selecionável, como links ou botões, pois isso pode causar problemas de navegação e entendimento do usuário sobre o que acontece ao clicar no componente.

## ♿ Acessibilidade

O componente `Icon` foi desenvolvido e testado para atender às diretrizes WCAG, oferecendo suporte a propriedades de acessibilidade e práticas recomendadas para garantir uma experiência inclusiva para todos os usuários.

- Quando o `Card` possui um texto ou título simples no cabeçalho, ele é automaticamente associado à área do card, melhorando a compreensão do conteúdo. Estando alinhado com a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/info-and-relationships.html" target="_blank">WCAG 1.3.1 (Info and Relationships)</Link>.
- Em seu estado de carregamento, o `Card` define a propriedade `aria-busy`, isso ajuda a indicar que o conteúdo está sendo carregado e melhora a acessibilidade para usuários de leitores de tela. Conforme a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/aria-busy.html" target="_blank">WCAG 4.1.2 (Name, Role, Value)</Link>.

Na versão interativa, foram considerados os seguintes pontos focados em acessibilidade:

- O `Card` interativo é totalmente navegável via teclado, a partir do `role=button` e `tabindex=0` permitindo o uso da tecla **Tab** para foco e **Enter** ou **Espaço** para ativação, conforme a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/keyboard.html" target="_blank">WCAG 2.1.1 (Keyboard)</Link>;
- A borda de foco visível possui pelo menos 2px de espessura e está sempre visível, sem ficar oculta por outros elementos, conforme <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG22/Understanding/focus-appearance-enhanced" target="_blank">WCAG 2.4.13 (Focus Appearance  - Enhanced)</Link>;
- O estado de foco é sempre preservado quando aplicável, inclusive com suporte visual claro, conforme recomendado pela <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/focus-visible" target="_blank">WCAG 2.4.7 (Focus Visible)</Link>;
- Quando desativado, as diretrizes de contraste de cores não se aplicam aos componentes desativados. As <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html#inactive-controls" target="_blank">diretrizes de contraste mínimo definidas nas WCAG 2.1</Link> declaram explicitamente que os componentes desativados estão isentos da diretriz;

### 📌 Boas práticas de acessibilidade ao utilizar o componente

Para complementar e garantir a acessibilidade, é importante considerar:

- Em casos onde sejam inseridos diferentes elementos interativos dentro do `Card`, mantenha uma sequência lógica de foco, garantindo que os usuários possam navegar facilmente entre os elementos. Conforme a <Link className="sb-unstyled text-sm" href="https://www.w3.org/WAI/WCAG21/Understanding/focus-order" target="_blank">WCAG 2.4.3 (Focus Order)</Link>.